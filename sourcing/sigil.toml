[project]
name = "windwalker-sourcing"
version = "0.1.0"
edition = "2025"
sigil-version = "0.4.0"
description = "Data sourcing pipeline for Native Treaty Mapping Initiative"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Daemoniorum-LLC/windwalker"

[dependencies]
# Core Daemoniorum frameworks (paths relative to dev root)
# These will be adjusted when we integrate properly

[features]
default = ["kappler", "nara"]
kappler = []
nara = []
bia = []
full = ["kappler", "nara", "bia"]

[compile]
backend = "llvm"
target-cpu = "native"

[dev-dependencies]
# Testing utilities
